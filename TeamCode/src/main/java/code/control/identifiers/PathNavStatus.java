package code.control.identifiers;

public enum PathNavStatus {
    START, PRELOAD, PARK,
    ;
    public enum Sample {
        SAMPLE1, SAMPLE2, SAMPLE3, SAMPLE_ZONE,
    }
    ;
    public enum Specimen {
        SPEC1, SPEC2, SPEC3, SPEC_ZONE, HOOK1, HOOK2, HOOK3, HOOK4,
    }
    ;
}
